# Waves

Seven agents in six waves, 2026-09-17, plus the coordinator's own planning. The subject was `plugins/entrust/skills/orchestrate/SKILL.md` and `plugins/entrust/skills/codex/SKILL.md` at `dff2f0b`; the inputs were one context measurement, one split critique, three surveys and one retrospective, merged by one agent. Errors each wave made are recorded beside what it produced, because a round that only records its findings cannot be weighed. Times and token counts are the coordinator's from the agent reports.

| Wave | Who | Produced | Got wrong |
|---|---|---|---|
| Measurement | 1 Opus (M1): 46 sessions, 8,446 API turns, 17 minutes, 131k tokens | the context breakdown — skill pages 2.1 %, agent returns 0.6 %, the orchestrator's own output 45.5 % (thinking 25.8 %, tool-call inputs 10.3 %, edits 5.1 %, text 4.3 %), inline command output 16.4 % — and the per-session and cross-session tables | the brief's proportional-split method produced an incoherent calibration and the agent replaced it with a two-pool split; 14.7 % of context growth stays unexplained |
| Planning | the coordinator (Fable, the main conversation) | the grilled design, three rounds, closed by the owner's "use the recommended" | first proposed the research as context reduction with a page dry-run, until the owner restated the goal; the first taxonomy (seven coordinator-mistake classes) mixed agent errors with coordinator decisions and C0 rejected it; two briefs carried a web-search mode the device's managed policy forbids, so two agents were launched twice (the refused launches ran no turn and cost nothing); a shell substitution slip while copying two prompt files cost one retry and nothing else; no completeness critic was spawned in this round either, the omission R12 records for every earlier run |
| Split critique | 1 Codex Astra (C0): 4 minutes, 182k tokens | "accept with corrections": eight decision stages instead of seven mistake classes; the page mapping moved out of the surveys into the merge; the rule that phase 1 does not measure improvement; the 2026-09-12 "Got wrong" cells classified — two the coordinator's own, three agent errors, two caught | — |
| Surveys | 1 Opus (S1: 60 rows, 23 sources, 26 minutes, 257k tokens, every quote machine-verified); 1 Codex Sol (S2: 32 + 16 rows, 18 arXiv sources, 27 minutes, 17.8M tokens mostly cached input, exit 6 with a complete answer); 1 Codex Sol (S3: 80 rows, 13 frameworks, 16 minutes, 7.9M tokens, exit 6, answer complete) | 188 rows with quotes, conditions, comparators and limitations; S2's coverage note that no fetched study tested a persistent advisor thread; S3's list of mechanisms absent from every framework; all 22 quotes re-fetched by the merge matched | S2 twice and S3 once tried to write outside its temporary directory, each refused by the sandbox; S3's ADK deprecation note is outside the question; both were relaunched once for the web-search header, the coordinator's error; S1 handed two measurements to S2 in its own text (S1-16, S1-52) and S2 fetched neither |
| Retrospective | 1 Opus (T1): 20 minutes, 292k tokens | 54 incidents with traces, the contribution-by-tier table, 27 owner corrections, the cross-check of the 2026-09-12 rounds.md | nothing found on the count: 54 rows in the file, ids T1-01 to T1-54 consecutive with no gap or duplicate, plus four unknowns; the stage sums 6 / 7 / 6 / 5 / 17 / 7 / 3 / 3 and severity sums 4 / 22 / 14 / 14 re-derived from the table by the merge; found by the design critic the same day: §2.1, §2.4 and T1-45 say two Astra verdicts in the naming round were overturned by a check where the transcript shows one (J3), the other reaffirmed and then set aside by the owner (T1-37's own row is right); and §2.4's medians are upper-middle values, not the conventional median (Astra 903,705 against 585,186) |
| Merge | 1 Fable (R): 30 minutes, 297k tokens | 185 merged claims; 495 line citations and 148 page quotes script-verified against `dff2f0b`; the mapping; the shortlist; the four verdicts; 15 hypotheses | its return cited O L120 and C L181 without naming the revision beside them: at `dff2f0b` they are the completeness critic and the `EXPECT:` row, in the working tree, which moved during the round (+3 lines after O L78, +6 by C L175), the same numbers land on the perspective-diverse bullet and the header-fields table header — the working-tree lines are O L123 and C L187; its §7 said seven prompt-only rows where its own table has nine (corrected in the scratch copy); its §1.4 totals were first typed from an estimate and replaced by a script count before hand-back; its return called S1-39 against O L25 a strict contradiction, where O L25 forbids grading one's own work and O L27 with the bounds section already allow inline checks of agents' work — an apparent contradiction on the coordinator's re-reading, recorded in the README |
| Design | 1 Fable (D1): 20 minutes, 272k tokens, then a 13-minute revision, 386k | eleven page deltas, ten new pins and two rewritten, a changelog entry, the line ledger (155 → 156), a measurement plan for Luna (H16–H18) | v1 banned a panel for a whole task where the evidence supports the decisive check first; reassigned work automatically after a rights refusal; carried two false measured claims (the Astra median, judge J2 "overturned"); grew the page by 534 words under a line-counted pin; its pins tested phrasing, not rules — all corrected in v2 (+251 words, alternations over the rule-carrying words, a guard against self-grading) |
| Design critique | 1 Codex Astra (C1): 12 minutes, 3.6M tokens | 2 keep / 9 fix / 0 drop; three blocks named; the pins probed (8 of 8 rewordings failed, 2 of 2 false measurements passed); the two false facts found by a recount over 78 reports and the transcript | — |

## Publication

The agents' artifacts were published as copies with two mechanical redactions: the owner's work project's path slug reads `<work-project>` and home paths read `~/`; nothing else was changed, and the originals stayed in the coordinator's temporary directory. `main` moved to `7e7d9cc` (entrust 0.17.0) during the round; the mapping is at `dff2f0b`, and at `7e7d9cc` O L123 is the completeness critic and C L187 the `EXPECT:` row (checked). The README's three decisions carry the owner's reading of 2026-09-17: the first dissolved into a recommendation to keep L25, the second into a narrow spawn rule, the third into three Luna hypotheses.

## What the round did not settle

Nothing in this round measures improvement. Untested by any source or run: a persistent advisor thread against one-shot briefs (theme b); bulk width against fewer strong readers (theme c, T1-U2); a capacity model for a mixed pool, and the owner's weights against per-token prices (theme d); the effect of a role table as such (theme a). Not done: no dry-run of the page; no re-derivation of the context measurement; the six reference files read by headings only; spot-check coverage 7 of 97 sources; Claude agents' token costs unrecorded; Terra never measured; S1-16 and S1-52 un-audited. Awaiting the owner: S1-39 against O L25; the completeness critic named at O L120 and never spawned; the "four times cheaper" at O L68 with no Haiku token trace.
